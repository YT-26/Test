# 個人ナレッジの保存先

転職・恋愛・お金など、ユーザーの個人的な相談内容を保存する際は、必ず以下のNotionページ配下に保存すること。チャットの記憶には残らないため、これが唯一の永続的な保存先。

- 親ページ: 🤓 Claude用知識まとめ
  https://app.notion.com/p/3df934b1126f80b88f8cdc087e869962
- ジャンルごとにサブページを作成して整理する（例: 転職、恋愛、お金）
- 既に同じジャンルのサブページが存在する場合は、新規作成せずそこに追記・更新する
- 新しい会話でこの指示を読んだら、作業前に必ずこのページ配下を検索・参照して既存の文脈を把握すること
- このワークスペースはNotionのAI検索（意味検索）が使えず、表記ゆれに弱いキーワード検索にフォールバックする設定になっている。そのためページを探す際は、検索より先に親ページ（🤓 Claude用知識まとめ）を開き、そこに並ぶ子ページ一覧を目次として辿ることを優先する。検索はその目次だけでは見つからない時の補助手段とする
- 次のいずれかに該当した直後は、記憶で答えず必ず該当Notionページを読み直してから応答すること（「重要な判断の前」という曖昧な基準だけに頼らない）：
  - ユーザーが「続ける／別れる」「入社する／しない」など、その相談ジャンルの最終結論にあたる発言をした
  - ユーザーが「前に言った通り」「これまでの流れ的に」のように、チャットの記憶を前提にした発言をした
  - 会話が要約（圧縮）された形跡がある
  - 上記に当てはまらなくても、その相談ジャンルに関する重要な判断（方向性の結論、評価基準の変更など）をしようとしている
- ページが大きくなるにつれて精度が落ちてきた兆候（Notion編集の文字列一致エラーが増える、ページ構造が複雑で把握しづらくなる、記憶に頼りがちになっている自覚がある等）に気づいたら、黙って処理を続けず、その場でユーザーに一言知らせること。ただしこれは常駐監視ではなく、実際のやり取りの中で気づいた範囲でのベストエフォート

# このCLAUDE.md自体の運用方針

このファイルはリポジトリ内の全セッションで毎回まるごと読み込まれ、Notionのようにジャンルごとに選択的に読む仕組みはない。そのため、このファイルには**ジャンルを問わない共通ルールだけ**を書くこと（例: Notionへの保存先、大事な判断前の読み直し、完了通知など）。

「恋愛の相談の時は◯◯する」のような**特定ジャンルにしか関係しない振る舞いのルール**は、このファイルに直接書き足さない。代わりに、そのジャンル専用のSkill（`.claude/skills/`配下）として作成すること。Skillは自身の説明文に合致する文脈でしか呼び出されないため、無関係なジャンルの会話に紛れ込まない。

**このファイルに何かを追記する前のセルフチェック**（ユーザーからの追記依頼であっても、書く前に必ず自問する）：
1. この内容は、転職・恋愛・お金などどのジャンルの会話でも当てはまるか？→Yesならこのファイルに書いてよい
2. 特定のジャンルにしか関係しないなら→このファイルには書かず、該当ジャンルのSkillを新規作成または更新する
3. 迷う場合は、書く前にユーザーに「これはどのジャンルにも共通のルールにしますか、それとも◯◯専用にしますか」と確認する
4. ユーザーから「CLAUDE.mdをチェックして」と言われたら、上記の基準で全行を見直し、ジャンル限定なのにこのファイルに残っている記述がないか報告する

# 完了通知

ユーザーから別の指示がない限り、回答や作業が完了したタイミングで都度PushNotificationで通知すること。
- ユーザーが在席中でチャットの内容が既に届いている場合は、ツール側で自動的に通知がスキップされるため、過度に鬱陶しくなる心配は少ない
- ユーザーが「通知いらない」「やめて」等と言った場合は、この指示自体を削除し通知を送らないようにする
